{
  "name": "generationali",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "start": "npm run start -w web",
    "lint": "npm run lint -w web"
  },
  "description": "Monorepo: UI app in apps/web; backend services in services/ (gateway, auth, document-ingestion, document-processing, rag, ai, summaries-history, billing, audit). Build the web app first; services are stubs until implemented."
}
